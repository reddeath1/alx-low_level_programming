#include "main.h"

/**
 *_abs - Main function that returns absolute value of a number
 * @r: The integre input
 * Return: (Always)
 */

int _abs(int r)
{
	if (r >= 0)
		return (r);
	else
		return (r * -1);
}
